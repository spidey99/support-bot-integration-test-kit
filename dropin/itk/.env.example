# ITK Environment Configuration (Tier-3 Live Execution)
# This is for running LIVE integration tests against real AWS QA resources.
# Copy to .env and fill in real values.

# =============================================================================
# MODE: live (Tier-3 executes against real AWS — NO MOCKS)
# =============================================================================
ITK_MODE=live

# =============================================================================
# AWS Credentials (choose one method)
# =============================================================================
# Option A: AWS SSO Profile
AWS_PROFILE=

# Option B: IAM Access Keys (less preferred)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=

# Region
AWS_REGION=us-east-1

# =============================================================================
# Environment (which AWS account/stage)
# =============================================================================
ITK_ENV=qa

# =============================================================================
# Entrypoint Configuration
# =============================================================================
ITK_DEFAULT_ENTRYPOINT=sqs

# SQS (golden path entrypoint)
ITK_SQS_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/ACCOUNT/QUEUE_NAME

# Lambda (for direct invoke / component tests)
ITK_TARGET_LAMBDA_ARN=arn:aws:lambda:us-east-1:ACCOUNT:function:FUNCTION_NAME

# Bedrock Agent (if using agent invoke)
ITK_BEDROCK_AGENT_ID=
ITK_BEDROCK_AGENT_ALIAS_ID=

# =============================================================================
# CloudWatch Logs
# =============================================================================
# Comma-separated log group names to query
ITK_LOG_GROUPS=/aws/lambda/my-handler,/aws/lambda/my-other-handler

# Query window (how far back to look for logs after test execution)
ITK_LOG_QUERY_WINDOW_SECONDS=900

# =============================================================================
# Resolver (optional: dynamic target resolution)
# =============================================================================
# Command to run before each test to refresh targets
# ITK_RESOLVER_CMD=python scripts/resolve_targets.py

# Path where resolver writes targets JSON (or use stdout)
# ITK_RESOLVER_OUTPUT=.itk/targets.json

# =============================================================================
# Correlation / Tracing
# =============================================================================
# Message attribute name for injected trace ID
ITK_TRACE_ID_ATTR=ITK_TRACE_ID

# =============================================================================
# Redaction (PII protection)
# =============================================================================
# Comma-separated keys to redact from payloads
ITK_REDACT_KEYS=password,ssn,credit_card,api_key,secret

# =============================================================================
# Rate Limiting (for soak tests)
# =============================================================================
# ITK_SOAK_INTERVAL=5
# ITK_SOAK_MAX_INFLIGHT=3

# =============================================================================
# Paths
# =============================================================================
ITK_ARTIFACTS_DIR=artifacts
ITK_CASES_DIR=cases

# =============================================================================
# Logging
# =============================================================================
ITK_LOG_LEVEL=INFO

# =============================================================================
# REMINDER: This mode executes LIVE tests. No mocks. No fallbacks.
# =============================================================================
