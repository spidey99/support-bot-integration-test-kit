# ITK Environment Configuration (Tier-3 Live Execution)
# ⚠️  DO NOT USE THIS FILE DIRECTLY - Copy to .env first!
# ⚠️  ALL values marked FIXME: must be replaced with real values!

# =============================================================================
# MODE: live (Tier-3 executes against real AWS — NO MOCKS)
# =============================================================================
ITK_MODE=live

# =============================================================================
# AWS Credentials (choose one method)
# =============================================================================
# Option A: AWS SSO Profile (uncomment and set)
# AWS_PROFILE=your-sso-profile-name

# Option B: Session credentials from CloudShell (uncomment all 3)
# Get these by running in CloudShell: aws configure export-credentials --format env
# Then REMOVE the 'export ' prefix when pasting here!
# AWS_ACCESS_KEY_ID=FIXME_paste_without_export_prefix
# AWS_SECRET_ACCESS_KEY=FIXME_paste_without_export_prefix
# AWS_SESSION_TOKEN=FIXME_paste_without_export_prefix

# Region
AWS_REGION=us-east-1

# =============================================================================
# Environment (which AWS account/stage)
# =============================================================================
ITK_ENV=qa

# =============================================================================
# Entrypoint Configuration
# =============================================================================
ITK_DEFAULT_ENTRYPOINT=sqs

# SQS (golden path entrypoint) - FIXME: replace with real queue URL
# ITK_SQS_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/123456789012/my-queue

# Lambda (for direct invoke / component tests) - FIXME: replace with real ARN
# ITK_TARGET_LAMBDA_ARN=arn:aws:lambda:us-east-1:123456789012:function:my-function

# Bedrock Agent (if using agent invoke) - FIXME: replace with real agent ID
# ITK_BEDROCK_AGENT_ID=ABCDEFGHIJ
# ITK_BEDROCK_AGENT_ALIAS_ID=TSTALIASID

# =============================================================================
# CloudWatch Logs
# =============================================================================
# Comma-separated log group names to query
# FIXME: Replace with your actual log group names! Run 'itk discover' to find them.
# ITK_LOG_GROUPS=/aws/lambda/FIXME-your-function-name

# Query window (how far back to look for logs after test execution)
ITK_LOG_QUERY_WINDOW_SECONDS=900

# =============================================================================
# Resolver (optional: dynamic target resolution)
# =============================================================================
# Command to run before each test to refresh targets
# ITK_RESOLVER_CMD=python scripts/resolve_targets.py

# Path where resolver writes targets JSON (or use stdout)
# ITK_RESOLVER_OUTPUT=.itk/targets.json

# =============================================================================
# Correlation / Tracing
# =============================================================================
# Message attribute name for injected trace ID
ITK_TRACE_ID_ATTR=ITK_TRACE_ID

# =============================================================================
# Redaction (PII protection)
# =============================================================================
# Comma-separated keys to redact from payloads
ITK_REDACT_KEYS=password,ssn,credit_card,api_key,secret

# =============================================================================
# Rate Limiting (for soak tests)
# =============================================================================
# ITK_SOAK_INTERVAL=5
# ITK_SOAK_MAX_INFLIGHT=3

# =============================================================================
# Paths
# =============================================================================
ITK_ARTIFACTS_DIR=artifacts
ITK_CASES_DIR=cases

# =============================================================================
# Logging
# =============================================================================
ITK_LOG_LEVEL=INFO

# =============================================================================
# REMINDER: This mode executes LIVE tests. No mocks. No fallbacks.
# =============================================================================
