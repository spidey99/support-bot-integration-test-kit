id: agent_gatekeeper_basic
name: Basic Bedrock Agent gatekeeper flow
fixture: ../fixtures/logs/agent_gatekeeper_flow.jsonl
entrypoint:
  type: bedrock_invoke_agent
  target:
    mode: invoke_agent
    agent_id: "AGENT_ID"
    agent_alias: "ALIAS"
  payload:
    inputText: "What is the status of my order?"
    sessionId: "session-001"
expected:
  invariants:
    - name: has_spans
    - name: has_entrypoint
notes:
  source: "test fixture for agent flow"
