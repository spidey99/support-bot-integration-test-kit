id: lambda_direct_invoke
name: Direct Lambda invocation test
fixture: ../fixtures/logs/sample_run_001.jsonl
entrypoint:
  type: lambda_invoke
  target:
    mode: invoke_lambda
    target_arn_or_url: "REPLACE_ME"
  payload:
    action: "process"
    data:
      key: "value"
expected:
  invariants:
    - name: has_spans
    - name: valid_timestamps
notes:
  source: "test fixture for direct Lambda"
