id: sqs_basic_message
name: Basic SQS message processing
fixture: ../fixtures/logs/sample_run_001.jsonl
entrypoint:
  type: sqs_event
  target:
    mode: invoke_lambda
    target_arn_or_url: "REPLACE_ME"
  payload:
    Records:
      - messageId: "msg-001"
        body:
          request:
            userMessage: "hello world"
expected:
  invariants:
    - name: has_spans
    - name: no_duplicate_span_ids
notes:
  source: "test fixture"
