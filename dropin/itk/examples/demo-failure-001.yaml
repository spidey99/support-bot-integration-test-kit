id: demo-failure-001
name: Demo failure - expects no errors but has errors
fixture: ../fixtures/logs/failure_demo.jsonl
entrypoint:
  type: sqs_event
  target:
    mode: invoke_lambda
    target_arn_or_url: "REPLACE_ME"
  payload:
    Records:
      - messageId: "00000000-0000-0000-0000-000000000000"
        body:
          request:
            userMessage: "hello"
expected:
  invariants:
    - name: has_spans
    - name: no_error_spans  # This will fail - fixture has errors
notes:
  source: "demo fixture"
  purpose: "demonstrates failure indicator in suite report"
