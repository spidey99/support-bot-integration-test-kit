# ITK: Add a new entrypoint adapter

Goal: add a new adapter under `dropin/itk/src/itk/entrypoints/`.

Steps:
- Implement adapter class
- Add unit tests using fixtures (no AWS)
- Add docs snippet in `docs/02-test-case-format.md`
- Add a sample case YAML using the new adapter
