# Planning (Tier 2)

This folder is for the **home/offline** implementation work.

Rules:
- Only generic code and public API usage.
- No work identifiers, no ARNs, no internal domain names.
- Prefer fixtures + schema validation + deterministic unit tests.
